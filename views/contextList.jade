extends layout.jade

block content
   each contextItems, value in contexts
      h3= value
      each item in contextItems
         div.row
            div.eight.columns               
               a(href='/context/view/' + item.id)
                  = item.title